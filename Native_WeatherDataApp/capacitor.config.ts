import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Weather_AirQualityTracker',
  webDir: 'www'
};

export default config;
